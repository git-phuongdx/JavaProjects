import com.sun.javafx.css.converters.StringConverter;
import com.sun.xml.internal.ws.api.model.wsdl.WSDLOutput;
import org.w3c.dom.ls.LSOutput;

import java.util.Scanner;

public class Bai4 {
    public static void main(String[] args) {
        int a , b , c;
        long myValue ;
        boolean isTrue;
        char character = '1' ;
        double myDoubleNumber ;
        /**  int firstNumber = 100;
        int secondNumber = 255;
        int sum = firstNumber + secondNumber;
        System.out.printf("SUM of two numbers is : " + firstNumber + secondNumber); **/


        /** int randomNumber = (int)(Math.random() * 101 );
        Scanner scn = new Scanner(System.in);
        System.out.println("Enter your number here:");
        int myNumber = scn.nextInt();
        if (randomNumber > myNumber) {
            System.out.println("Your number: " + myNumber + "\t smaller than" + "\t" + randomNumber);
        }else {
            System.out.println("Your number: " + myNumber + "\t greater than" + "\t" + randomNumber);
        } **/
    }
}
